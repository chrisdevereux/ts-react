machine:
  node:
    version: 6.3.1

test:
  override:
    - "npm run test:ci":
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml

deployment:
  staging:
    branch: master
    heroku:
      appname: ts-react-staging
